package aoc2020

class Day17Test {
	private lateinit var cut: Day17

	/*    @BeforeEach
			fun setup() {
					cut = Day17()

					val input = listOf(".#.", "..#", "###" )
					cut.buildWorld(input);
			}

			@Test
			fun initWith5activeCubes() {
					assertEquals(5, cut.world.size)
			}

			@Test
			fun cycle1CoordToCheck() {
					val result = cut.calcCoordsToCheck()
					assertEquals(66, result.size)
			}

			@Test
			fun cycle1() {
					cut.cycleWorld()
					// cut.printWorld()
					assertEquals(11, cut.world.size)
			}
	*/

}
